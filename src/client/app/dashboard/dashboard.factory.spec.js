/**
 * Created by sergiomarcial on 9/3/16.
 */
/* jshint -W117, -W030 */
'use strict';

/*
describe('dashboard factory', function () {

  describe('get Wordpress Posts', function () {

    var httpBackend;

    beforeEach(function () {
      module('app.dashboard', bard.fakeToastr);

      bard.inject('$httpBackend', 'dashboardFactory', '$rootScope');
      console.log($httpBackend);
      var response = {data: {posts: [{URL: 'TestURL', title: 'TestTitle'}]}};

      httpBackend = $httpBackend.when('GET', 'http://localhost:3000/api/wordpress/posts')
        .respond(200, response);

    });

    bard.verifyNoOutstandingHttpRequests();

    it('should get wordpress posts', function () {
      expect(dashboardFactory).to.be.defined;
      dashboardFactory.getWordpressPosts('testToken').then(function (result) {
        expect(result).to.be.defined;
      });
    });

  });

});
*/
